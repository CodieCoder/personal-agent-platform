{
  "name": "@pap/runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @pap/memory build && pnpm --filter @pap/ai build && pnpm --filter @pap/tools-search build && pnpm --filter @pap/tools-web build && pnpm --filter @pap/source-profiles build && tsc -b tsconfig.json",
    "typecheck": "pnpm --filter @pap/memory build && pnpm --filter @pap/ai build && pnpm --filter @pap/tools-search build && pnpm --filter @pap/tools-web build && pnpm --filter @pap/source-profiles build && tsc --noEmit -p tsconfig.json",
    "lint": "biome lint .",
    "test": "pnpm run build && vitest run test/runtime.test.mjs"
  },
  "dependencies": {
    "@pap/ai": "workspace:*",
    "@pap/contracts": "workspace:*",
    "@pap/memory": "workspace:*",
    "@pap/shared": "workspace:*",
    "@pap/source-profiles": "workspace:*",
    "@pap/storage": "workspace:*",
    "@pap/tools-search": "workspace:*",
    "@pap/tools-web": "workspace:*"
  }
}
