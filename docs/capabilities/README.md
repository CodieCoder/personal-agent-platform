# Capability Docs

Use this folder for capability-specific design and behavior docs.

The first implemented core capability is `@pap/capability-echo`. Its runtime skill files live in
`packages/capabilities/echo/skills/echo/` because product runtime skills belong with their owning
capability package.
